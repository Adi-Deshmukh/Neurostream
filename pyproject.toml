[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "neurostream"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["numpy>=1.24", "scipy>=1.10", "mne>=1.5", "moabb>=1.1"]

[tool.pytest.ini_options]
pythonpath = ["src"]
